import React from 'react';
import BootcampList from '../components/BootcampList';

const Enroll = () => {
    return (
        <div>
            <h2>Enroll in a Bootcamp</h2>
            <BootcampList />
        </div>
    );
};

export default Enroll;
